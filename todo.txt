* standardize using immutable references everywhere. If this is a pain in the ass, use Cow
* learn how to profile cloning.
* Test for deduping of signs by category id when adding an association.
* Hook up the rest endpoints (read) to the aggregate and/or projection.
* Unbox and parameterize everything possible.
* use erased_serializer to store a serializer in the client itself rather than hard-coding json.